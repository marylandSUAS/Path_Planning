function randomizeStatic(start,finish,number)

    fileID = fopen('static_obstacles.txt','w');
    for k = 1:number 
        
        z = rand()*50+50;
        r = rand()*12+3;
        dis = rand()*2-1;
        x = start(1)+(rand()*(finish(1)-start(1))/2)+dis*r*cos(theta);
        y = start(2)+rand()*(finish(1)-start(1))/2+dis*r*co(theta);
        
        
        fprintf(fileID,'%4.2f %4.2f %4.2f %4.2f %4.2f %4.2f %4.2f\n',[]);
    end
    fclose(fileID);
end
